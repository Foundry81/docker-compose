###
# Docmost Application Configuration
###

# Public URL where Docmost is accessed.
# Example: https://docs.example.com
APP_URL=https://docs.example.com

# Application secret used for encryption, signing, and internal security.
# Generate a long, random value (32+ characters).
# Example:
#   openssl rand -hex 32
APP_SECRET=replace-with-a-secure-random-string


###
# Database Configuration (PostgreSQL)
###

# Database name used by Docmost
POSTGRES_DB=docmost

# Database user Docmost connects as
POSTGRES_USER=docmost

# Password for the PostgreSQL user.
# Use a strong, unique password.
POSTGRES_PASSWORD=replace-with-a-strong-db-password

# Full database connection string used by Docmost.
# This should match the credentials above.
DATABASE_URL=postgresql://docmost:${POSTGRES_PASSWORD}@db:5432/docmost?schema=public


###
# Redis Configuration
###

# Redis connection string.
# Defaults are fine unless Redis auth is enabled.
REDIS_URL=redis://redis:6379
